# CodeAlpha_Network_Sniffer
Python-based Network Sniffer project using Scapy that captures live network packets and extracts source and destination IP addresses. It also logs packet data into a file for analysis.
